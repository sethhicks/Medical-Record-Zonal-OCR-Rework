---
phase: 02-image-pipeline-coordinate-calibration
plan: 02
type: execute
wave: 1
depends_on:
  - "02-01"
files_modified:
  - config_loader.py
autonomous: true
requirements:
  - PROC-02

must_haves:
  truths:
    - "load_settings() returns threshold_block_size=31 when settings.json is absent"
    - "load_settings() returns user-supplied threshold_block_size when present in settings.json"
    - "All existing Phase 1 settings keys are still present (no regression)"
  artifacts:
    - path: "config_loader.py"
      provides: "threshold_block_size in _DEFAULTS"
      contains: "threshold_block_size"
  key_links:
    - from: "config_loader.py"
      to: "pipeline/preprocessor.py"
      via: "settings.get('threshold_block_size', 31)"
      pattern: "threshold_block_size"
---

<objective>
Extend config_loader.py to add threshold_block_size=31 to the _DEFAULTS dict.

Purpose: The preprocessor reads this key via settings.get("threshold_block_size", 31). Per D-09 and ENV-02, all tunables must flow through the settings schema rather than hardcoded fallbacks. This is the minimal change that unlocks the preprocessor's settings integration.

Output: config_loader.py with threshold_block_size key in _DEFAULTS; test_settings_threshold_block_size_default passes green.
</objective>

<execution_context>
@C:/Users/shset/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shset/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-image-pipeline-coordinate-calibration/02-CONTEXT.md
@.planning/phases/02-image-pipeline-coordinate-calibration/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add threshold_block_size to _DEFAULTS in config_loader.py</name>
  <files>config_loader.py</files>

  <read_first>
    - config_loader.py (current state — read the full file; the _DEFAULTS dict is at lines 11–16; only add one key, change nothing else)
    - .planning/phases/02-image-pipeline-coordinate-calibration/02-PATTERNS.md §config_loader.py section (exact apply-as block showing the modified _DEFAULTS)
  </read_first>

  <action>
Read config_loader.py in full first. Then make exactly one change: add `"threshold_block_size": 31` as the last entry in the `_DEFAULTS` dict.

The modified `_DEFAULTS` block must look like this:

```python
_DEFAULTS: dict = {
    "tesseract_cmd": r"C:\Program Files\Tesseract-OCR\tesseract.exe",
    "poppler_path": r"C:\Program Files\poppler\Library\bin",
    "confidence_threshold": 60,
    "output_dir": str(pathlib.Path.home() / "Desktop"),
    "threshold_block_size": 31,
}
```

No other changes to config_loader.py. Do not modify the `load_settings()` function, docstring, imports, or error handling. The existing merge logic (`{**_DEFAULTS, **user}`) already handles the new key correctly:
- When settings.json is absent: returns `_DEFAULTS.copy()` which includes the new key
- When settings.json exists but omits the key: merge produces default value
- When settings.json sets a custom value: user value overrides default

Per D-09: default is 31. Per T-2-03 (security): the preprocessor validates that block_size is odd at runtime (`if block_size % 2 == 0: block_size += 1`), so no input validation needed here.
  </action>

  <verify>
    <automated>cd /c/Users/shset/Documents/GitHub/OCR-Rework && python -m pytest tests/test_phase2.py::test_settings_threshold_block_size_default -v</automated>
  </verify>

  <acceptance_criteria>
    - `grep -v "^#" config_loader.py | grep -c "threshold_block_size"` returns at least 1
    - `grep "threshold_block_size" config_loader.py` contains `31`
    - `python -m pytest tests/test_phase2.py::test_settings_threshold_block_size_default -v` exits 0 (PASSED)
    - `python -m pytest tests/test_phase1.py -x -q` exits 0 (all 8 Phase 1 tests still pass)
    - `python -c "from config_loader import load_settings; s = load_settings(path='nonexistent.json'); assert s['threshold_block_size'] == 31"` exits 0
  </acceptance_criteria>

  <done>test_settings_threshold_block_size_default passes green. All 8 Phase 1 tests still pass. threshold_block_size=31 is in _DEFAULTS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.json → _DEFAULTS merge | User-supplied threshold_block_size value flows into preprocessor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-02-01 | Tampering | config_loader.py — threshold_block_size from settings.json | mitigate | Preprocessor validates odd block_size at runtime: `if block_size % 2 == 0: block_size += 1` (T-2-03 from security_context). config_loader.py does not validate — validation is at point of use. |
</threat_model>

<verification>
```bash
cd /c/Users/shset/Documents/GitHub/OCR-Rework
python -m pytest tests/test_phase2.py::test_settings_threshold_block_size_default tests/test_phase1.py -v
```
Expected: test_settings_threshold_block_size_default PASSED, all 8 Phase 1 tests PASSED.
</verification>

<success_criteria>
- config_loader.py _DEFAULTS contains "threshold_block_size": 31
- test_settings_threshold_block_size_default passes
- Phase 1 regression: 8/8 green
</success_criteria>

<output>
After completion, create `.planning/phases/02-image-pipeline-coordinate-calibration/02-02-SUMMARY.md`
</output>
