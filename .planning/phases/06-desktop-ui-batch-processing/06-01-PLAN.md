---
phase: 06-desktop-ui-batch-processing
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_phase6.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - PROC-04

must_haves:
  truths:
    - "tests/test_phase6.py exists and is importable by pytest"
    - "All stubs are skipped (0 failures, 0 errors) when the suite runs"
    - "Stub names cover every acceptance criterion for Phase 6"
  artifacts:
    - path: "tests/test_phase6.py"
      provides: "Skipped stub tests for all Phase 6 acceptance criteria"
      contains: "@pytest.mark.skip"
  key_links:
    - from: "tests/test_phase6.py"
      to: "main.py"
      via: "imports inside test function bodies (deferred import pattern)"
      pattern: "import main"
---

<objective>
Create the Phase 6 test scaffold: tests/test_phase6.py with 12 skipped stub tests covering all acceptance criteria for the desktop UI, batch processing, and integration behavior. Stubs follow the identical pattern established in Phases 3-5 — @pytest.mark.skip decorator removed by later plans as each feature is implemented.

Purpose: Establish the full test surface before any implementation so Wave 1 and Wave 2 plans have concrete targets to activate.
Output: tests/test_phase6.py with 12 stubs, 0 active tests. Entire prior suite still passes.
</objective>

<execution_context>
@C:\Users\shset\Documents\GitHub\OCR-Rework\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\shset\Documents\GitHub\OCR-Rework\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-desktop-ui-batch-processing/06-CONTEXT.md
</context>

<interfaces>
<!-- Pattern from tests/test_phase3.py and tests/test_phase4.py -->

Stub pattern (used in all prior phases):
```python
@pytest.mark.skip(reason="Phase 6 stub")
def test_example():
    """Description of what this verifies."""
    pass
```

Active test pattern (deferred imports inside function body):
```python
def test_example():
    import main
    # assertions here
```

conftest.py fixture available:
```python
@pytest.fixture
def test_pdf_path():
    return str(Path(__file__).parent.parent / "test.pdf")
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_phase6.py with 12 skipped stubs</name>
  <files>tests/test_phase6.py</files>
  <read_first>
    - tests/test_phase3.py (stub pattern: @pytest.mark.skip, deferred imports inside function bodies)
    - tests/test_phase4.py (stub header comment listing which plan removes each stub)
    - .planning/phases/06-desktop-ui-batch-processing/06-CONTEXT.md (D-01 through D-14 — every stub must map to a locked decision)
    - .planning/ROADMAP.md (Phase 6 success criteria — stubs must cover all 5)
  </read_first>
  <action>
Create tests/test_phase6.py with exactly the structure below. Every test is decorated with @pytest.mark.skip(reason="Phase 6 stub"). The header comment maps each stub to the plan that removes it. Imports are deferred inside function bodies (not at module level) to avoid import errors before main.py is implemented.

File content to create:

```python
# tests/test_phase6.py
"""Phase 6 tests — UI-01, UI-02, UI-03, UI-04, PROC-04 (desktop UI & batch processing).

Skip markers are removed by Wave 1/2 plans as each feature is implemented:
  - test_startup_check_blocks_ui        -> removed by 06-02-PLAN
  - test_window_non_resizable           -> removed by 06-02-PLAN
  - test_select_pdfs_populates_listbox  -> removed by 06-02-PLAN
  - test_clear_empties_file_list        -> removed by 06-02-PLAN
  - test_output_label_shows_path        -> removed by 06-02-PLAN
  - test_queue_progress_message_format  -> removed by 06-02-PLAN
  - test_queue_error_message_format     -> removed by 06-02-PLAN
  - test_queue_done_message_format      -> removed by 06-02-PLAN
  - test_worker_unknown_page_posts_error -> removed by 06-02-PLAN
  - test_import_main                    -> removed by 06-02-PLAN
  - test_worker_smoke_real_pdf          -> removed by 06-03-PLAN
  - test_batch_multi_pdf_produces_output -> removed by 06-03-PLAN
"""
import pytest
from pathlib import Path

_ROOT = Path(__file__).parent.parent
_TEST_PDF = str(_ROOT / "test.pdf")


# ---------------------------------------------------------------------------
# UI-01: File selection
# ---------------------------------------------------------------------------

@pytest.mark.skip(reason="Phase 6 stub")
def test_select_pdfs_populates_listbox():
    """Selecting PDFs via filedialog populates the Listbox with basenames (D-06)."""
    pass


@pytest.mark.skip(reason="Phase 6 stub")
def test_clear_empties_file_list():
    """[Clear] button empties the Listbox and internal file list (D-07)."""
    pass


# ---------------------------------------------------------------------------
# UI-02: Progress display / threading model
# ---------------------------------------------------------------------------

@pytest.mark.skip(reason="Phase 6 stub")
def test_queue_progress_message_format():
    """Worker posts ('progress', current_page, total_pages) tuples to the queue (D-10)."""
    pass


@pytest.mark.skip(reason="Phase 6 stub")
def test_queue_done_message_format():
    """Worker posts ('done', output_path) when all pages are processed (D-10)."""
    pass


# ---------------------------------------------------------------------------
# UI-03: Open output file
# ---------------------------------------------------------------------------

@pytest.mark.skip(reason="Phase 6 stub")
def test_output_label_shows_path():
    """Output label text matches 'Output: {output_dir}\\extracted_results.xlsx' (D-05)."""
    pass


# ---------------------------------------------------------------------------
# UI-04: Error display
# ---------------------------------------------------------------------------

@pytest.mark.skip(reason="Phase 6 stub")
def test_queue_error_message_format():
    """Worker posts ('error', filename, page_num, error_message) for failed pages (D-12)."""
    pass


@pytest.mark.skip(reason="Phase 6 stub")
def test_worker_unknown_page_posts_error():
    """UNKNOWN-classified pages post an error tuple with 'UNKNOWN form type' message (D-14)."""
    pass


# ---------------------------------------------------------------------------
# ENV / startup check
# ---------------------------------------------------------------------------

@pytest.mark.skip(reason="Phase 6 stub")
def test_startup_check_blocks_ui():
    """run_checks() is called at startup; returns bool used to gate Tk() construction (D-02)."""
    pass


@pytest.mark.skip(reason="Phase 6 stub")
def test_window_non_resizable():
    """root.resizable(False, False) is present in main.py (D-03)."""
    pass


# ---------------------------------------------------------------------------
# Import smoke
# ---------------------------------------------------------------------------

@pytest.mark.skip(reason="Phase 6 stub")
def test_import_main():
    """main module is importable without side effects when __name__ != '__main__'."""
    pass


# ---------------------------------------------------------------------------
# PROC-04: Batch / integration (activated by 06-03-PLAN)
# ---------------------------------------------------------------------------

@pytest.mark.skip(reason="Phase 6 stub")
def test_worker_smoke_real_pdf(test_pdf_path):
    """Worker function processes all 30 pages of test.pdf without raising (real Tesseract, ~60s)."""
    pass


@pytest.mark.skip(reason="Phase 6 stub")
def test_batch_multi_pdf_produces_output(test_pdf_path, tmp_path):
    """Running worker with two copies of test.pdf produces extracted_results.xlsx with data rows."""
    pass
```
  </action>
  <verify>
    <automated>cd /c/Users/shset/Documents/GitHub/OCR-Rework && python -m pytest tests/test_phase6.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_phase6.py exists
    - Running pytest on tests/test_phase6.py shows 0 failed, 0 errors, 12 skipped
    - grep -c "@pytest.mark.skip" tests/test_phase6.py returns 10 (the two integration tests each have one decorator; total stubs = 12 but the 2 at the bottom also use @pytest.mark.skip — verify all 12 function definitions have skip decorators)
    - Full prior suite (tests/test_phase1.py through test_phase5.py) still passes: python -m pytest tests/ --ignore=tests/test_phase6.py exits 0
    - File contains the string "Phase 6 stub" (decorator reason text)
    - File contains "_TEST_PDF = str(_ROOT / \"test.pdf\")" (root path setup)
  </acceptance_criteria>
  <done>tests/test_phase6.py exists with 12 skipped stubs. pytest reports 0 passed, 12 skipped, 0 errors. Full suite (60 prior tests + 12 skipped stubs) exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → filesystem | Test file reads project root path via __file__; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | tests/test_phase6.py | accept | Test file contains no secrets; _TEST_PDF path is local only; low-value target |
</threat_model>

<verification>
Run the full test suite to confirm no regressions:

```bash
cd /c/Users/shset/Documents/GitHub/OCR-Rework && python -m pytest tests/ -v --tb=short
```

Expected: 60 passed (phases 1-5), 12 skipped (phase 6 stubs), 0 errors.
</verification>

<success_criteria>
1. tests/test_phase6.py exists with exactly 12 skipped stubs
2. pytest tests/ exits 0 — no failures, no errors
3. Stub names match the plan-removal comments in the header
4. All five Phase 6 requirement IDs (UI-01 through UI-04, PROC-04) are represented by at least one stub
</success_criteria>

<output>
After completion, create .planning/phases/06-desktop-ui-batch-processing/06-01-SUMMARY.md
</output>
