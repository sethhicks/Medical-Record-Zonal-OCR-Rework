---
phase: 04-field-extraction-cms-1500-ub-04
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01-PLAN.md
files_modified:
  - config/cms1500.py
autonomous: true
requirements:
  - EXTR-01

must_haves:
  truths:
    - "box21a_diag through box21l_diag whitelists include uppercase alpha characters so ICD-10 codes like F32.9 are captured"
    - "box24_cpt whitelist includes hyphen so hyphenated CPT modifiers are captured"
    - "No other field definitions (name, box, psm, label) are altered"
    - "All Phase 4 skipped stubs still collected; suite remains green"
  artifacts:
    - path: "config/cms1500.py"
      provides: "Updated whitelist values for ICD-10 and CPT fields"
      contains: "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "
  key_links:
    - from: "config/cms1500.py"
      to: "pipeline/extractor_cms1500.py"
      via: "fd.whitelist read by _ocr_region config string builder"
      pattern: "whitelist.*ABCDEFGHIJKLMNOPQRSTUVWXYZ"
---

<objective>
Update config/cms1500.py whitelist values for box21a–l (ICD-10) and box24_cpt (CPT/HCPCS) per locked decisions D-02 and D-03.

Purpose: The extractor reads fd.whitelist directly; if this update is skipped, alpha ICD-10 codes like F32.9 will be silently truncated to ".9" and CPT hyphenated codes will drop the hyphen. This must land before any extractor code is written.

Output: config/cms1500.py with 13 whitelist values updated (12 × box21x_diag + 1 × box24_cpt).
</objective>

<execution_context>
@C:/Users/shset/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shset/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/shset/Documents/GitHub/OCR-Rework/.planning/ROADMAP.md
@C:/Users/shset/Documents/GitHub/OCR-Rework/.planning/STATE.md

<interfaces>
<!-- config/cms1500.py current structure (extracted from source) -->

From config/cms1500.py lines 9–44 (single fields) and lines 110–123 (box24_cpt TableFieldDef):

box21a_diag through box21l_diag — all currently have:
  whitelist="0123456789. "
  Must be changed to: whitelist="ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "

box24_cpt TableFieldDef at approximately line 111–123 — currently has:
  whitelist="0123456789"
  Must be changed to: whitelist="0123456789- "

FieldDef schema (change ONLY the whitelist= argument, nothing else):
  FieldDef(name=..., box=(...), psm=N, whitelist="...", label="...")

TableFieldDef schema:
  TableFieldDef(name=..., row_boxes=[...], psm=N, whitelist="...", label="...")

Exact affected entries in CMS1500_FIELDS:
  Line 24: box21a_diag   whitelist="0123456789. "   -> "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "
  Line 25: box21b_diag   whitelist="0123456789. "   -> "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "
  Line 26: box21c_diag   whitelist="0123456789. "   -> "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "
  Line 27: box21d_diag   whitelist="0123456789. "   -> "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "
  Line 28: box21e_diag   whitelist="0123456789. "   -> "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "
  Line 29: box21f_diag   whitelist="0123456789. "   -> "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "
  Line 30: box21g_diag   whitelist="0123456789. "   -> "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "
  Line 31: box21h_diag   whitelist="0123456789. "   -> "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "
  Line 32: box21i_diag   whitelist="0123456789. "   -> "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "
  Line 33: box21j_diag   whitelist="0123456789. "   -> "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "
  Line 34: box21k_diag   whitelist="0123456789. "   -> "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "
  Line 35: box21l_diag   whitelist="0123456789. "   -> "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "

Affected entry in CMS1500_TABLE_FIELDS:
  box24_cpt TableFieldDef whitelist="0123456789" -> "0123456789- "
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update box21a–l and box24_cpt whitelists in config/cms1500.py</name>
  <files>config/cms1500.py</files>

  <read_first>
  - config/cms1500.py — read the full file to see current whitelist values and confirm exact line numbers before any edit; do not assume line numbers from this plan match exactly
  - .planning/phases/04-field-extraction-cms-1500-ub-04/04-CONTEXT.md — D-02 (ICD-10 whitelist), D-03 (CPT whitelist)
  </read_first>

  <action>
Update config/cms1500.py with exactly two categories of whitelist changes. Read the file first to locate exact line numbers; then apply:

**Change 1 — 12 entries in CMS1500_FIELDS (D-02, per locked decision):**
For each of these field names: box21a_diag, box21b_diag, box21c_diag, box21d_diag, box21e_diag, box21f_diag, box21g_diag, box21h_diag, box21i_diag, box21j_diag, box21k_diag, box21l_diag
Change:
  whitelist="0123456789. "
To:
  whitelist="ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. "

**Change 2 — 1 entry in CMS1500_TABLE_FIELDS (D-03, per locked decision):**
For the TableFieldDef with name="box24_cpt"
Change:
  whitelist="0123456789"
To:
  whitelist="0123456789- "

**Constraints:**
- Change ONLY the whitelist= argument for these 13 entries
- Do NOT alter name=, box=, psm=, or label= for any entry
- Do NOT change any other field's whitelist (box17b, box23, box25, box24_pos, box24_emg, box24_modifier, box24_diag_ptr, box24_charges, box24_units, box24_rendering_npi must not be touched)
- The file must still import cleanly: `python -c "from config.cms1500 import CMS1500_FIELDS, CMS1500_TABLE_FIELDS; print(len(CMS1500_FIELDS), len(CMS1500_TABLE_FIELDS))"` must print `29 10`
  </action>

  <verify>
    <automated>cd /c/Users/shset/Documents/GitHub/OCR-Rework && python -c "from config.cms1500 import CMS1500_FIELDS, CMS1500_TABLE_FIELDS; diag=[f for f in CMS1500_FIELDS if 'box21' in f.name]; cpt=[t for t in CMS1500_TABLE_FIELDS if t.name=='box24_cpt'][0]; print('diag count:', len(diag)); print('diag wl:', diag[0].whitelist); print('cpt wl:', cpt.whitelist); print('field count:', len(CMS1500_FIELDS), len(CMS1500_TABLE_FIELDS))"</automated>
  </verify>

  <acceptance_criteria>
  - `grep -c "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789\. " config/cms1500.py` returns 12 (the 12 box21x_diag entries)
  - `grep "box24_cpt" config/cms1500.py` output contains `whitelist="0123456789- "`
  - `python -c "from config.cms1500 import CMS1500_FIELDS, CMS1500_TABLE_FIELDS; print(len(CMS1500_FIELDS), len(CMS1500_TABLE_FIELDS))"` prints `29 10`
  - `python -c "from config.cms1500 import CMS1500_FIELDS; f=[x for x in CMS1500_FIELDS if x.name=='box21a_diag'][0]; assert f.whitelist=='ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. ', f.whitelist; print('OK')"` prints OK
  - `python -m pytest tests/ -q` exits 0 (31 passing, 17 skipped)
  </acceptance_criteria>

  <done>config/cms1500.py has ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. whitelist on all 12 box21x_diag entries and "0123456789- " on box24_cpt; module imports cleanly; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → extractor | Whitelist strings from config are passed as Tesseract CLI config strings; no user input, no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02 | Tampering | config/cms1500.py whitelist strings | accept | Config is a developer-controlled source file; whitelist strings are passed verbatim to Tesseract (not shell-interpolated); no user input path |
| T-4-03 | Information Disclosure | Tesseract whitelist config string | accept | Whitelist is a data-quality constraint, not a security boundary; OCR output is consumed internally only, never exposed to web or external callers |
</threat_model>

<verification>
```
python -c "from config.cms1500 import CMS1500_FIELDS, CMS1500_TABLE_FIELDS; diag=[f for f in CMS1500_FIELDS if 'box21' in f.name]; assert all(f.whitelist=='ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. ' for f in diag), 'box21 whitelist wrong'; cpt=[t for t in CMS1500_TABLE_FIELDS if t.name=='box24_cpt'][0]; assert cpt.whitelist=='0123456789- ', cpt.whitelist; print('All whitelist checks passed')"
python -m pytest tests/ -q
```
</verification>

<success_criteria>
- All 12 box21x_diag FieldDef entries have whitelist="ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789. " (D-02)
- box24_cpt TableFieldDef has whitelist="0123456789- " (D-03)
- No other field definitions altered
- Full test suite green (31 passing, 17 skipped)
</success_criteria>

<output>
After completion, create `.planning/phases/04-field-extraction-cms-1500-ub-04/04-02-SUMMARY.md`
</output>
