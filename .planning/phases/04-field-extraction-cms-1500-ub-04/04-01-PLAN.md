---
phase: 04-field-extraction-cms-1500-ub-04
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_phase4.py
autonomous: true
requirements:
  - EXTR-01
  - EXTR-02
  - EXTR-03

must_haves:
  truths:
    - "tests/test_phase4.py exists with exactly 17 test function definitions"
    - "All 17 tests carry @pytest.mark.skip so the full suite remains green"
    - "Full test suite (31 existing + 17 new skipped = 48 collected) exits 0"
  artifacts:
    - path: "tests/test_phase4.py"
      provides: "Wave 0 stub file for all Phase 4 tests"
      min_lines: 80
  key_links:
    - from: "tests/test_phase4.py"
      to: "tests/conftest.py"
      via: "test_pdf_path and sample_settings fixtures"
      pattern: "def test_.*test_pdf_path|def test_.*sample_settings"
---

<objective>
Create tests/test_phase4.py with all 17 Phase 4 test stubs decorated @pytest.mark.skip.

Purpose: Establishes the Nyquist contract before any implementation. Wave 1 plans remove skip markers one batch at a time as extractors are built. Wave 2 removes integration test markers after empirical calibration.

Output: tests/test_phase4.py (17 skipped stubs) — full suite stays green at 31 passing + 17 skipped = 48 collected.
</objective>

<execution_context>
@C:/Users/shset/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shset/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/shset/Documents/GitHub/OCR-Rework/.planning/PROJECT.md
@C:/Users/shset/Documents/GitHub/OCR-Rework/.planning/ROADMAP.md
@C:/Users/shset/Documents/GitHub/OCR-Rework/.planning/STATE.md

<interfaces>
<!-- Fixtures available from tests/conftest.py — no changes needed to conftest.py -->
<!-- test_pdf_path: session-scoped, returns str path to test.pdf in project root -->
<!-- sample_settings: session-scoped, returns load_settings() dict with defaults -->

From tests/conftest.py:
```python
@pytest.fixture(scope="session")
def test_pdf_path() -> str:
    path = Path(__file__).parent.parent / "test.pdf"
    assert path.exists(), f"test.pdf not found at {path}"
    return str(path)

@pytest.fixture(scope="session")
def sample_settings(tmp_path_factory) -> dict:
    from config_loader import load_settings
    tmp = tmp_path_factory.mktemp("settings")
    return load_settings(path=str(tmp / "settings.json"))
```

Stub pattern from tests/test_phase3.py:
```python
@pytest.mark.skip(reason="stub — implemented by Wave 1")
def test_extract_cms1500_returns_list(monkeypatch):
    ...
```

All imports inside test function bodies (avoids ImportError when module not yet created).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_phase4.py with 17 skipped stubs</name>
  <files>tests/test_phase4.py</files>

  <read_first>
  - tests/test_phase3.py — structural template: file docstring format, _ROOT/_TEST_PDF constants, import-inside-function pattern, @pytest.mark.skip usage, monkeypatch pattern for image_to_data mocking
  - tests/conftest.py — available fixtures: test_pdf_path (str), sample_settings (dict)
  - .planning/phases/04-field-extraction-cms-1500-ub-04/04-RESEARCH.md — complete list of 17 test names in "Phase Requirements → Test Map" table
  </read_first>

  <action>
Create tests/test_phase4.py following the exact structural template of tests/test_phase3.py. The file must contain exactly 17 stub functions in the order listed below, each decorated with `@pytest.mark.skip(reason="stub — implemented by Wave 1")` EXCEPT the two integration stubs which use `reason="stub — implemented by Wave 2"`.

**File header:**
```python
# tests/test_phase4.py
"""Phase 4 tests — EXTR-01, EXTR-02, EXTR-03 (field extraction).

Skip markers are removed by Wave 1/2 plans as each feature is implemented:
  - test_extract_cms1500_returns_list          -> removed by 04-03-PLAN
  - test_extract_cms1500_result_count          -> removed by 04-03-PLAN
  - test_extract_cms1500_single_field_names    -> removed by 04-03-PLAN
  - test_extract_cms1500_service_line_naming   -> removed by 04-03-PLAN
  - test_extract_cms1500_blank_row_sentinel    -> removed by 04-03-PLAN
  - test_cms1500_all_results_have_confidence   -> removed by 04-03-PLAN
  - test_extract_ub04_returns_list             -> removed by 04-04-PLAN
  - test_extract_ub04_result_count             -> removed by 04-04-PLAN
  - test_extract_ub04_revenue_line_naming      -> removed by 04-04-PLAN
  - test_extract_ub04_blank_rl_sentinel        -> removed by 04-04-PLAN
  - test_ub04_all_results_have_confidence      -> removed by 04-04-PLAN
  - test_import_extract_cms1500_from_pipeline  -> removed by 04-05-PLAN
  - test_import_extract_ub04_from_pipeline     -> removed by 04-05-PLAN
  - test_cms1500_whitelist_npi_chars           -> removed by 04-06-PLAN
  - test_ub04_whitelist_icd10_chars            -> removed by 04-06-PLAN
  - test_cms1500_smoke_80pct                   -> removed by 04-06-PLAN
  - test_ub04_smoke_80pct                      -> removed by 04-06-PLAN
"""
import pytest
from pathlib import Path

_ROOT = Path(__file__).parent.parent
_TEST_PDF = str(_ROOT / "test.pdf")
```

**The 17 stub functions to create** (all decorated with @pytest.mark.skip; Wave 1 stubs use `reason="stub — implemented by Wave 1"`, Wave 2 stubs use `reason="stub — implemented by Wave 2"`):

Unit stubs (Wave 1 reason):
1. `test_extract_cms1500_returns_list(monkeypatch)` — "extract_cms1500 returns a list"
2. `test_extract_cms1500_result_count(monkeypatch)` — "extract_cms1500 returns exactly 89 FieldResults (29 single + 60 table)"
3. `test_extract_cms1500_single_field_names(monkeypatch)` — "all 29 single-field names are present in the result"
4. `test_extract_cms1500_service_line_naming(monkeypatch)` — "Box 24 table entries use _sl1.._sl6 suffix (D-06)"
5. `test_extract_cms1500_blank_row_sentinel(monkeypatch)` — "blank region returns FieldResult with confidence=-1.0 (D-11)"
6. `test_cms1500_all_results_have_confidence(monkeypatch)` — "every CMS-1500 FieldResult has a numeric confidence attribute"
7. `test_extract_ub04_returns_list(monkeypatch)` — "extract_ub04 returns a list"
8. `test_extract_ub04_result_count(monkeypatch)` — "extract_ub04 returns exactly 178 FieldResults (24 single + 154 table)"
9. `test_extract_ub04_revenue_line_naming(monkeypatch)` — "revenue line entries use _rl1.._rl22 suffix (D-07)"
10. `test_extract_ub04_blank_rl_sentinel(monkeypatch)` — "blank RL region returns FieldResult with confidence=-1.0 (D-11)"
11. `test_ub04_all_results_have_confidence(monkeypatch)` — "every UB-04 FieldResult has a numeric confidence attribute"
12. `test_import_extract_cms1500_from_pipeline()` — "extract_cms1500 importable from pipeline (D-09)"
13. `test_import_extract_ub04_from_pipeline()` — "extract_ub04 importable from pipeline (D-09)"

Integration stubs (Wave 2 reason):
14. `test_cms1500_whitelist_npi_chars(test_pdf_path, sample_settings)` — "CMS-1500 NPI/CPT fields contain only whitelisted chars"
15. `test_ub04_whitelist_icd10_chars(test_pdf_path, sample_settings)` — "UB-04 ICD-10 fields contain only alpha+digit+dot"
16. `test_cms1500_smoke_80pct(test_pdf_path, sample_settings)` — "real CMS-1500 page: ≥empirically-calibrated % non-empty fields"
17. `test_ub04_smoke_80pct(test_pdf_path, sample_settings)` — "real UB-04 page: ≥empirically-calibrated % non-empty fields"

Each stub body must contain `pass` only — no implementation. All imports must be deferred inside function bodies (not at module level) so the file can be collected even before the pipeline modules exist.

The monkeypatch stubs must include `monkeypatch` in their signature even though they use `pass` — this preserves the fixture binding for when the skip is removed.

The integration stubs (14-17) must include `test_pdf_path, sample_settings` in their signatures.
  </action>

  <verify>
    <automated>cd /c/Users/shset/Documents/GitHub/OCR-Rework && python -m pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - `tests/test_phase4.py` exists
  - `grep -c "def test_" tests/test_phase4.py` returns 17
  - `grep -c "pytest.mark.skip" tests/test_phase4.py` returns 17
  - `python -m pytest tests/ -q` exits 0 with output containing "31 passed, 17 skipped" (or "48 collected")
  - `grep "test_cms1500_smoke_80pct" tests/test_phase4.py` returns a line with `reason="stub — implemented by Wave 2"`
  - `grep "test_import_extract_cms1500_from_pipeline" tests/test_phase4.py` returns a line with `reason="stub — implemented by Wave 1"`
  </acceptance_criteria>

  <done>tests/test_phase4.py exists with 17 @pytest.mark.skip stubs; full pytest suite exits 0 with 31 passing + 17 skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → no boundary | Test scaffold only; no external inputs, no I/O, no trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | tests/test_phase4.py | accept | Test file is developer-only artifact in a local repo; no untrusted input; low-value target |
</threat_model>

<verification>
Run after task completion:
```
python -m pytest tests/ -q
```
Expected: 31 passed, 17 skipped, 0 errors.
</verification>

<success_criteria>
- tests/test_phase4.py exists with exactly 17 test stubs
- All 17 stubs decorated with @pytest.mark.skip
- Full pytest suite exits 0 (31 passing, 17 skipped)
- No import errors on collection (all imports are deferred inside function bodies)
</success_criteria>

<output>
After completion, create `.planning/phases/04-field-extraction-cms-1500-ub-04/04-01-SUMMARY.md`
</output>
