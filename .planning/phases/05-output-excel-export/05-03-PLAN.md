---
phase: 05-output-excel-export
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - pipeline/__init__.py
  - tests/test_phase5.py
autonomous: true
requirements:
  - OUT-01
  - OUT-02
  - OUT-03
  - OUT-04
  - OUT-05

must_haves:
  truths:
    - "from pipeline import write_workbook imports cleanly"
    - "write_workbook is present in pipeline.__all__"
    - "All 12 Phase 5 tests pass (0 skipped)"
    - "Full suite is 60 passed, 0 skipped, 0 errors"
    - "text-format (@) verified on NPI column and date column via real workbook inspection"
    - "One data row per page written to correct sheet"
  artifacts:
    - path: "pipeline/__init__.py"
      provides: "write_workbook added to public API"
      contains: "write_workbook"
    - path: "tests/test_phase5.py"
      provides: "All 12 stubs activated (0 remaining @pytest.mark.skip)"
      min_lines: 80
  key_links:
    - from: "pipeline/__init__.py"
      to: "pipeline/writer.py"
      via: "from .writer import write_workbook"
      pattern: "from .writer import write_workbook"
---

<objective>
Wire write_workbook into the pipeline public API and activate the 4 remaining integration test stubs.

Purpose: Makes write_workbook importable as `from pipeline import write_workbook` — the import pattern Phase 6 will use. Activating the integration stubs confirms text-format columns and per-page row counts work end-to-end.

Output: pipeline/__init__.py updated with write_workbook export; all 12 Phase 5 stubs activated; full suite 60 passed.
</objective>

<execution_context>
@C:\Users\shset\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\shset\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\shset\Documents\GitHub\OCR-Rework\.planning\ROADMAP.md
@C:\Users\shset\Documents\GitHub\OCR-Rework\.planning\phases\05-output-excel-export\05-CONTEXT.md

<interfaces>
<!-- Prior plan outputs consumed here. Extracted from codebase. -->

From pipeline/__init__.py (current state before this plan):
```python
from .converter import convert_page
from .preprocessor import preprocess_page
from .detector import detect_form_type
from .extractor_cms1500 import extract_cms1500
from .extractor_ub04 import extract_ub04

__all__ = [
    "convert_page",
    "preprocess_page",
    "detect_form_type",
    "extract_cms1500",
    "extract_ub04",
]
```

From pipeline/writer.py (created by 05-02-PLAN):
```python
def write_workbook(
    cms_pages: list[list[FieldResult]],
    ub_pages: list[list[FieldResult]],
    settings: dict,
) -> str: ...
```

Extractor field_name naming conventions (needed for integration test setup):
  CMS-1500 table fields: f"{tfd.name}_sl{i+1}" — e.g. "box24_date_from_sl1"
  UB-04 table fields:    f"{tfd.name}_rl{i+1}" — e.g. "ub04_rl_rev_code_rl1"
  CMS-1500 NPI field: "box17b_referring_npi" (single field) or "box24_rendering_npi_sl1" (table)
  UB-04 NPI field:    "box56_npi"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add write_workbook to pipeline/__init__.py</name>
  <files>pipeline/__init__.py</files>
  <read_first>
    - pipeline/__init__.py (current state — must read before editing to avoid overwriting existing exports)
    - pipeline/writer.py (confirm the exact function name to import)
  </read_first>
  <action>
Edit pipeline/__init__.py to add write_workbook. The final file must contain:

1. Updated module docstring — add `write_workbook(cms_pages, ub_pages, settings) -> str` to the "Exposes:" list.

2. New import line after the existing five imports:
```python
from .writer import write_workbook
```

3. Updated __all__ — expand to 6-name multiline list (same formatting style as current 5-name list):
```python
__all__ = [
    "convert_page",
    "preprocess_page",
    "detect_form_type",
    "extract_cms1500",
    "extract_ub04",
    "write_workbook",
]
```

Do NOT change any existing import line or remove any existing export. Preserve the existing module docstring structure and add write_workbook to the Exposes list.
  </action>
  <verify>
    <automated>cd /c/Users/shset/Documents/GitHub/OCR-Rework && python -c "from pipeline import write_workbook; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/__init__.py contains `from .writer import write_workbook`
    - pipeline/__init__.py contains `"write_workbook"` inside the __all__ list
    - `python -c "from pipeline import write_workbook; assert callable(write_workbook)"` exits 0
    - `python -c "import pipeline; assert 'write_workbook' in pipeline.__all__"` exits 0
    - All five prior exports still present: `python -c "from pipeline import convert_page, preprocess_page, detect_form_type, extract_cms1500, extract_ub04"` exits 0
  </acceptance_criteria>
  <done>write_workbook is importable from pipeline and listed in __all__.</done>
</task>

<task type="auto">
  <name>Task 2: Activate the 4 remaining integration stubs and verify full suite</name>
  <files>tests/test_phase5.py</files>
  <read_first>
    - tests/test_phase5.py (current state — must read to see exact stub bodies before removing skip markers)
    - pipeline/writer.py (confirm write_workbook signature and column map key format for test setup)
    - config/cms1500.py (confirm CMS1500_FIELDS[0].name = "claim_member_id" and CMS1500_TABLE_FIELDS[0].name = "box24_date_from" — needed to construct field_name for integration test FieldResults)
  </read_first>
  <action>
Remove `@pytest.mark.skip(reason="stub — activated by 05-03-PLAN")` from exactly these 4 test functions:
  - test_import_write_workbook_from_pipeline
  - test_text_format_npi_column
  - test_text_format_date_column
  - test_cms1500_one_row_per_page

After removing skip markers, verify each test body is correct for the implementation. The test bodies should already be correct from the Wave 0 scaffold; fix any assertion that would fail due to a mismatch between scaffold assumptions and actual implementation.

Key facts to verify the test bodies against:

**test_text_format_npi_column** — sets up a CMS-1500 page with a FieldResult whose field_name is "box17b_referring_npi" and value "0123456789". After write_workbook, opens the workbook with openpyxl. Finds the column index for "box17b_referring_npi" in the CMS-1500 sheet by iterating row 1 cells looking for header "Box 17b -- Referring NPI" (the label from CMS1500_FIELDS). Asserts that cell(row=2, column=that_col_idx).number_format == "@". If the stub body looked up by header text, ensure the header text matches exactly "Box 17b -- Referring NPI".

**test_text_format_date_column** — sets up a CMS-1500 page with a FieldResult whose field_name is "box24_date_from_sl1" (note: "_sl1" suffix matching extractor convention). After write_workbook, finds column for header "Box 24 -- Date From SL1" in row 1. Asserts cell number_format == "@". The expected header is `f"{tfd.label or tfd.name} SL{N}"` = "Box 24 -- Date From SL1".

**test_cms1500_one_row_per_page** — passes cms_pages as a list of 2 page results (each page is a list of FieldResults, can be empty lists). Asserts CMS-1500 sheet max_row == 3 (1 header row + 2 data rows).

**test_import_write_workbook_from_pipeline** — already straightforward: `from pipeline import write_workbook; assert callable(write_workbook)`.

Update the file docstring to show all 12 stubs have been activated by their respective plans.

Do NOT change any test logic that correctly matches the implementation. Only remove skip decorators and fix header string mismatches if any exist.
  </action>
  <verify>
    <automated>cd /c/Users/shset/Documents/GitHub/OCR-Rework && python -m pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Running `python -m pytest tests/ -q` reports 60 passed, 0 skipped, 0 errors
    - tests/test_phase5.py contains zero `@pytest.mark.skip` decorators (grep -c "@pytest.mark.skip" tests/test_phase5.py returns 0)
    - Running `python -m pytest tests/test_phase5.py -v` shows 12 PASSED, 0 skipped
    - test_text_format_npi_column PASSED (not ERROR — confirms header lookup "Box 17b -- Referring NPI" finds a column)
    - test_text_format_date_column PASSED (confirms header "Box 24 -- Date From SL1" is found and formatted @)
    - test_cms1500_one_row_per_page PASSED (confirms 2 pages -> max_row == 3)
  </acceptance_criteria>
  <done>All 12 Phase 5 stubs activated; full suite is 60 passed, 0 skipped, 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline public API → callers | write_workbook added to __all__; no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | pipeline/__init__.py __all__ | accept | __all__ is documentation only; no security boundary; write_workbook is a pure file-writing function with no auth |
| T-05-07 | Tampering | test_phase5.py integration tests | accept | Tests run in isolated tmp_path; no production data touched during CI |
</threat_model>

<verification>
Full suite: `python -m pytest tests/ -q`
Expected: 60 passed, 0 skipped, 0 errors

Phase 5 tests only: `python -m pytest tests/test_phase5.py -v`
Expected: 12 PASSED

API availability: `python -c "from pipeline import write_workbook; print(write_workbook.__module__)"`
Expected prints: `pipeline.writer`

Manual smoke (optional — confirms real workbook structure):
```python
python -c "
from pipeline.writer import write_workbook
import tempfile, pathlib, openpyxl
tmp = tempfile.mkdtemp()
path = write_workbook([], [], {'output_dir': tmp, 'confidence_threshold': 60})
wb = openpyxl.load_workbook(path)
print('Sheets:', wb.sheetnames)
print('CMS cols:', wb['CMS-1500'].max_column)
print('UB cols:', wb['UB-04'].max_column)
"
```
Expected: Sheets: ['CMS-1500', 'UB-04'], CMS cols: 89, UB cols: 178
</verification>

<success_criteria>
- write_workbook exported from pipeline (importable as `from pipeline import write_workbook`)
- write_workbook in pipeline.__all__
- All 12 Phase 5 tests pass
- Full suite: 60 passed, 0 skipped, 0 errors
- Phase 6 can call write_workbook without any additional wiring
</success_criteria>

<output>
After completion, create `.planning/phases/05-output-excel-export/05-03-SUMMARY.md`
</output>
