---
phase: 05-output-excel-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase5.py
autonomous: true
requirements:
  - OUT-01
  - OUT-02
  - OUT-03
  - OUT-04
  - OUT-05

must_haves:
  truths:
    - "tests/test_phase5.py exists with 12 @pytest.mark.skip stubs covering writer structure, column headers, yellow fill, text format, and file output"
    - "Running pytest reports 12 skipped, 0 errors alongside the existing 48 passed"
  artifacts:
    - path: "tests/test_phase5.py"
      provides: "Test scaffold for all OUT-01 through OUT-05 acceptance criteria"
      min_lines: 80
  key_links:
    - from: "tests/test_phase5.py"
      to: "pipeline.write_workbook"
      via: "from pipeline import write_workbook (deferred inside test bodies)"
      pattern: "from pipeline import write_workbook"
---

<objective>
Create the Wave 0 test scaffold for Phase 5. Stubs cover every acceptance criterion defined by OUT-01 through OUT-05 and the five roadmap success criteria.

Purpose: Establish the test contract before implementation. Each stub documents what the writer must produce; stubs are removed in Wave 2 as the implementation is wired.

Output: tests/test_phase5.py with 12 skipped stubs and a docstring index tracking which plan activates each stub.
</objective>

<execution_context>
@C:\Users\shset\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\shset\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\shset\Documents\GitHub\OCR-Rework\.planning\ROADMAP.md
@C:\Users\shset\Documents\GitHub\OCR-Rework\.planning\phases\05-output-excel-export\05-CONTEXT.md

<interfaces>
<!-- Key types the test scaffold uses. Extracted from codebase. -->

From models/field_result.py:
```python
@dataclass
class FieldResult:
    field_name: str    # matches FieldDef.name e.g. "box17b_referring_npi"
    value: str         # raw OCR text, stripped
    confidence: float  # 0.0-100.0; -1.0 if no text found in region
```

From config_loader.py:
```python
_DEFAULTS = {
    "confidence_threshold": 60,
    "output_dir": str(pathlib.Path.home() / "Desktop"),
}
def load_settings(path: str | None = None) -> dict: ...
```

CMS-1500 total FieldResults per page: 89 (29 single + 60 table)
UB-04 total FieldResults per page: 178 (24 single + 154 table)

CMS-1500 expected column count: 89 (same as FieldResult count — one column per result)
UB-04 expected column count: 178

CMS-1500 known NPI field names (for text-format test): "box17b_referring_npi", "box24_rendering_npi"
CMS-1500 known date field names (for text-format test): "box24_date_from", "box3_dob_sex"
UB-04 known NPI field name: "box56_npi"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_phase5.py with 12 skipped stubs</name>
  <files>tests/test_phase5.py</files>
  <read_first>
    - tests/test_phase4.py (read to replicate exact stub pattern: @pytest.mark.skip decorator, deferred import inside test body, _ROOT path constant, docstring activation index)
    - models/field_result.py (FieldResult fields used in test setup)
    - config_loader.py (_DEFAULTS: confidence_threshold=60, output_dir=Desktop)
  </read_first>
  <action>
Create tests/test_phase5.py following the exact pattern from test_phase4.py:

1. File docstring listing all 12 stubs and which plan removes the skip marker (05-02-PLAN activates unit stubs, 05-03-PLAN activates integration stubs).

2. Module-level constants:
```python
import pytest
from pathlib import Path
_ROOT = Path(__file__).parent.parent
```

3. Create a module-level helper `_make_results(field_names, value="", confidence=95.0)` that returns `list[FieldResult]` — this lets stubs build synthetic page data without repeating boilerplate.

4. All 12 stubs must use `@pytest.mark.skip(reason="stub — activated by 05-0N-PLAN")` and import `write_workbook` via `from pipeline import write_workbook` INSIDE the test function body (not at module level — avoids import errors before 05-03-PLAN wires the export).

**Stub list (write all 12):**

Unit stubs (activated by 05-02-PLAN, 8 stubs):
- `test_write_workbook_returns_str` — call write_workbook with empty cms_pages=[[]], ub_pages=[[]], settings dict; assert isinstance(result, str)
- `test_output_file_created` — call write_workbook, assert Path(result).exists()
- `test_two_sheets_named_correctly` — load workbook, assert sheetnames == ["CMS-1500", "UB-04"]
- `test_cms1500_column_count` — CMS-1500 sheet max_column == 89
- `test_ub04_column_count` — UB-04 sheet max_column == 178
- `test_cms1500_header_row_frozen` — ws.freeze_panes == "A2"
- `test_yellow_fill_below_threshold` — write a page with one FieldResult(confidence=10.0) below threshold=60; assert that cell's fill.fgColor.rgb == "FFFF00"
- `test_no_fill_above_threshold` — write a page with one FieldResult(confidence=90.0) above threshold=60; assert that cell's fill is None or PatternFill with fill_type="none" or fgColor not "FFFF00"

Integration stubs (activated by 05-03-PLAN, 4 stubs):
- `test_import_write_workbook_from_pipeline` — `from pipeline import write_workbook; assert callable(write_workbook)`
- `test_text_format_npi_column` — write page with FieldResult("box17b_referring_npi", "0123456789", 95.0); find column for that header in CMS-1500 sheet; assert cell.number_format == "@"
- `test_text_format_date_column` — write page with FieldResult("box24_date_from_sl1", "01/01/2024", 95.0); assert cell.number_format == "@"
- `test_cms1500_one_row_per_page` — write cms_pages with 2 pages; assert CMS-1500 sheet max_row == 3 (header + 2 data rows)

For each stub that calls write_workbook, use `tmp_path` fixture and pass `settings={"output_dir": str(tmp_path), "confidence_threshold": 60}`. Import openpyxl inside the test body to load and inspect the workbook.
  </action>
  <verify>
    <automated>cd /c/Users/shset/Documents/GitHub/OCR-Rework && python -m pytest tests/test_phase5.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_phase5.py exists and is valid Python (no syntax errors)
    - pytest --collect-only reports exactly 12 test items collected from test_phase5.py
    - Running `python -m pytest tests/ -q` reports 48 passed, 12 skipped, 0 errors
    - Every test function body contains `from pipeline import write_workbook` (not at module level)
    - File contains `@pytest.mark.skip` on all 12 test functions
    - File contains `_ROOT = Path(__file__).parent.parent`
  </acceptance_criteria>
  <done>12 skipped stubs created; full suite is 48 passed, 12 skipped, 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → filesystem | tmp_path writes during test execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | tests/test_phase5.py | accept | Test files contain no secrets; tmp_path is isolated per pytest invocation |
</threat_model>

<verification>
Run full suite: `python -m pytest tests/ -q`
Expected: 48 passed, 12 skipped, 0 errors
</verification>

<success_criteria>
- tests/test_phase5.py exists with exactly 12 @pytest.mark.skip stubs
- All stubs import write_workbook inside the test body (not module-level)
- Full suite passes: 48 passed, 12 skipped, 0 errors
- Each stub's skip reason references the plan that will activate it
</success_criteria>

<output>
After completion, create `.planning/phases/05-output-excel-export/05-01-SUMMARY.md`
</output>
