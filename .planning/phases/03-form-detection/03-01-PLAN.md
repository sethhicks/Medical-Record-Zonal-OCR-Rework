---
phase: 03-form-detection
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_phase3.py
autonomous: true
requirements:
  - PROC-03
must_haves:
  truths:
    - "tests/test_phase3.py exists with 8 test stubs, all skipped"
    - "pytest collects all 8 tests with no import errors"
    - "running the suite exits 0 (all skipped = green)"
  artifacts:
    - path: "tests/test_phase3.py"
      provides: "Phase 3 test scaffold with stubs for all PROC-03 behaviors"
      contains: "test_cms1500_detection_mock"
  key_links:
    - from: "tests/test_phase3.py"
      to: "tests/conftest.py"
      via: "test_pdf_path fixture (no re-declaration needed)"
      pattern: "def test_.*\\(test_pdf_path"
---

<objective>
Create `tests/test_phase3.py` with 8 skipped stub tests covering every PROC-03 behavior.
All stubs use `pytest.mark.skip` so the suite exits 0 immediately. Wave 1 plans remove
the skip markers as each feature is implemented.

Purpose: Establish the test contract before any production code exists. Executors know
exactly which behaviors they are implementing against.

Output: `tests/test_phase3.py` — 8 stubs, all skipped, suite green.
</objective>

<execution_context>
@C:/Users/shset/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shset/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Existing fixtures — do NOT re-declare these in test_phase3.py -->

From tests/conftest.py:
```python
@pytest.fixture(scope="session")
def test_pdf_path() -> str:
    """Absolute path to test.pdf in the project root."""
    path = Path(__file__).parent.parent / "test.pdf"
    assert path.exists(), f"test.pdf not found at {path}"
    return str(path)

@pytest.fixture(scope="session")
def sample_settings(tmp_path_factory) -> dict:
    from config_loader import load_settings
    tmp = tmp_path_factory.mktemp("settings")
    return load_settings(path=str(tmp / "settings.json"))
```

From tests/test_phase2.py (header pattern to replicate):
```python
# tests/test_phase2.py
"""Phase 2 tests — ..."""
import os
import subprocess
import sys
from pathlib import Path
import pytest

_ROOT = Path(__file__).parent.parent
_TEST_PDF = str(_ROOT / "test.pdf")

# ---------------------------------------------------------------------------
# PROC-01: convert_page
# ---------------------------------------------------------------------------
def test_convert_page_dimensions():
    from pipeline import convert_page  # deferred import inside function body
    ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_phase3.py with 8 skipped stubs</name>
  <read_first>
    - tests/test_phase2.py — copy file header, _ROOT/_TEST_PDF constants, section separator style, and deferred-import pattern exactly
    - tests/conftest.py — confirm fixture names; do NOT re-declare test_pdf_path or sample_settings
  </read_first>
  <files>tests/test_phase3.py</files>
  <action>
Create `tests/test_phase3.py` with the exact structure below. All 8 tests carry
`@pytest.mark.skip(reason="Wave 1 — detector not implemented yet")`.

File header (copy style from test_phase2.py):
```python
# tests/test_phase3.py
"""Phase 3 tests — PROC-03 (form type detection).

Skip markers are removed by Wave 1 plans as each feature is implemented:
  - test_cms1500_detection_mock     -> removed by 03-02-PLAN
  - test_ub04_detection_mock        -> removed by 03-02-PLAN
  - test_unknown_when_no_anchors    -> removed by 03-02-PLAN
  - test_both_match_returns_unknown -> removed by 03-02-PLAN
  - test_unknown_result_is_string   -> removed by 03-02-PLAN
  - test_import_from_pipeline       -> removed by 03-03-PLAN
  - test_cms1500_smoke              -> removed by 03-03-PLAN
  - test_ub04_smoke                 -> removed by 03-03-PLAN
"""
import pytest
from pathlib import Path

_ROOT = Path(__file__).parent.parent
_TEST_PDF = str(_ROOT / "test.pdf")
```

Section separator (copy style from test_phase2.py):
```
# ---------------------------------------------------------------------------
# PROC-03: detect_form_type
# ---------------------------------------------------------------------------
```

The 8 stub functions, each with `@pytest.mark.skip(reason="Wave 1 — detector not implemented yet")`:

1. `test_cms1500_detection_mock(monkeypatch)` — docstring: "2-of-3 CMS-1500 anchors via mock OCR returns 'CMS-1500'."
   Body: `pass`

2. `test_ub04_detection_mock(monkeypatch)` — docstring: "NUBC anchor found via mock OCR returns 'UB-04'."
   Body: `pass`

3. `test_unknown_when_no_anchors(monkeypatch)` — docstring: "All OCR returns empty string — result is 'UNKNOWN'."
   Body: `pass`

4. `test_both_match_returns_unknown(monkeypatch)` — docstring: "cms_score>=2 AND ub04_score>=1 simultaneously -> 'UNKNOWN'."
   Body: `pass`

5. `test_unknown_result_is_string(monkeypatch)` — docstring: "detect_form_type always returns a str; 'UNKNOWN' is str."
   Body: `pass`

6. `test_import_from_pipeline()` — docstring: "detect_form_type is importable from the pipeline package."
   Body: `pass`

7. `test_cms1500_smoke(test_pdf_path)` — docstring: "Real test.pdf page 0 classifies as 'CMS-1500' (uses actual Tesseract, ~2s)."
   Body: `pass`

8. `test_ub04_smoke(test_pdf_path)` — docstring: "Real test.pdf page 6 classifies as 'UB-04' (uses actual Tesseract, ~2s)."
   Body: `pass`

Rules:
- All `from pipeline import ...` calls must be inside test function bodies (deferred import pattern), NOT at module top level
- Use `test_pdf_path` as a fixture parameter (not `_TEST_PDF`) for tests 7 and 8
- Use `_TEST_PDF` or `_ROOT` only in tests with no fixture parameters if needed
- No `from pipeline import detect_form_type` at module level
  </action>
  <verify>
    <automated>cd C:/Users/shset/Documents/GitHub/OCR-Rework &amp;&amp; python -m pytest tests/test_phase3.py -v 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_phase3.py` exists
    - `grep -c "def test_" tests/test_phase3.py` outputs `8`
    - `grep -c "pytest.mark.skip" tests/test_phase3.py` outputs `8`
    - `grep "def test_cms1500_detection_mock" tests/test_phase3.py` matches
    - `grep "def test_ub04_detection_mock" tests/test_phase3.py` matches
    - `grep "def test_unknown_when_no_anchors" tests/test_phase3.py` matches
    - `grep "def test_both_match_returns_unknown" tests/test_phase3.py` matches
    - `grep "def test_unknown_result_is_string" tests/test_phase3.py` matches
    - `grep "def test_import_from_pipeline" tests/test_phase3.py` matches
    - `grep "def test_cms1500_smoke" tests/test_phase3.py` matches
    - `grep "def test_ub04_smoke" tests/test_phase3.py` matches
    - `pytest tests/test_phase3.py -v` exits 0 with "8 skipped" in output
    - `pytest tests/test_phase3.py -v` produces no ERRORS or FAILED lines
  </acceptance_criteria>
  <done>tests/test_phase3.py exists with 8 skipped stubs; `pytest tests/test_phase3.py` exits 0 showing "8 skipped"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → pytest | Test stubs execute no production code; no trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | tests/test_phase3.py | accept | Wave 0 file contains only stubs with `pass` bodies; no production logic; no sensitive data; low-value target |

ASVS level: L1 — no auth, no data storage, no network. This plan creates test stubs only.
</threat_model>

<verification>
`pytest tests/test_phase3.py -v` exits 0 showing "8 skipped, 0 failed, 0 errors".
`pytest tests/ -x` still passes (no regression to Phase 1/2 tests).
</verification>

<success_criteria>
- tests/test_phase3.py exists with exactly 8 test functions
- All 8 have @pytest.mark.skip decorator
- Full suite exits 0 with no failures or errors
- Section separator `# PROC-03: detect_form_type` present
</success_criteria>

<output>
After completion, create `.planning/phases/03-form-detection/03-01-SUMMARY.md`
</output>
